overwrite: true
schema: "./example/schema.graphql"
documents: null
generates:
  ./example/autogenerated-resolver-types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      enumsAsTypes: true
require:
  - ts-node/register
